name: CI + CD Pipeline

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout
      - name: Checkout code
        uses: actions/checkout@v6

      # 2. Login Docker Hub
      - name: Login Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 3. Backend
      - name: Build & Push Backend
        uses: docker/build-push-action@v7
        with:
          context: ./backend
          push: true
          tags: xuanho17102004/project-chat-social-backend:latest

      # 4. AI
      - name: Build & Push AI
        uses: docker/build-push-action@v7
        with:
          context: ./server-ai
          push: true
          tags: xuanho17102004/project-chat-social-server-ai:latest

      # 5. Kafka
      - name: Build & Push Kafka
        uses: docker/build-push-action@v7
        with:
          context: ./kafka-server
          push: true
          tags: xuanho17102004/project-chat-social-kafka-server:latest

      # 6. RabbitMQ
      - name: Build & Push Rabbit
        uses: docker/build-push-action@v7
        with:
          context: ./rabbitmq
          push: true
          tags: xuanho17102004/project-chat-social-rabbitmq-worker:latest

      # 7. Frontend
      - name: Build & Push Frontend
        uses: docker/build-push-action@v7
        with:
          context: ./frontend
          push: true
          tags: xuanho17102004/project-chat-social-frontend:latest
          build-args: |
            NEXT_PUBLIC_BACKEND_API_URL=${{ secrets.NEXT_PUBLIC_BACKEND_API_URL }}
            NEXT_PUBLIC_SOCKET_URL=${{ secrets.NEXT_PUBLIC_SOCKET_URL }}
            NEXT_PUBLIC_FIREBASE_API_KEY=${{ secrets.NEXT_PUBLIC_FIREBASE_API_KEY }}
            NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=${{ secrets.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}
            NEXT_PUBLIC_FIREBASE_PROJECT_ID=${{ secrets.NEXT_PUBLIC_FIREBASE_PROJECT_ID }}
            NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=${{ secrets.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}
            NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=${{ secrets.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}
            NEXT_PUBLIC_FIREBASE_APP_ID=${{ secrets.NEXT_PUBLIC_FIREBASE_APP_ID }}
            NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=${{ secrets.NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID }}
            NEXT_PUBLIC_FIREBASE_VAPID_KEY=${{ secrets.NEXT_PUBLIC_FIREBASE_VAPID_KEY }}

  deploy:
    needs: docker
    runs-on: self-hosted

    steps:
      # 1. Login Docker Hub
      - name: Login Docker Hub
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      # 2. Clone nếu chưa có (PRIVATE bằng TOKEN)
      - name: Prepare source code
        run: |
          if [ ! -d "/home/ubuntu/Project-Chat-Social/.git" ]; then
            git clone https://XuanHo1710:${{ secrets.SECRET_KEY }}@github.com/XuanHo1710/Project-Chat-Social.git /home/ubuntu/Project-Chat-Social
          fi
          cd /home/ubuntu/Project-Chat-Social
          git pull https://XuanHo1710:${{ secrets.SECRET_KEY }}@github.com/XuanHo1710/Project-Chat-Social.git main

      # 3. Write .env files from GitHub Secrets (không commit .env lên git)
      - name: Write .env files
        env:
          BACKEND_ENV: ${{ secrets.BACKEND_ENV }}
          KAFKA_SERVER_ENV: ${{ secrets.KAFKA_SERVER_ENV }}
          RABBITMQ_ENV: ${{ secrets.RABBITMQ_ENV }}
          SERVER_AI_ENV: ${{ secrets.SERVER_AI_ENV }}
          FRONTEND_ENV: ${{ secrets.FRONTEND_ENV }}
        run: |
          [ -n "$BACKEND_ENV" ]      && printf '%s' "$BACKEND_ENV"      > /home/ubuntu/Project-Chat-Social/backend/.env
          [ -n "$KAFKA_SERVER_ENV" ] && printf '%s' "$KAFKA_SERVER_ENV" > /home/ubuntu/Project-Chat-Social/kafka-server/.env
          [ -n "$RABBITMQ_ENV" ]     && printf '%s' "$RABBITMQ_ENV"     > /home/ubuntu/Project-Chat-Social/rabbitmq/.env
          [ -n "$SERVER_AI_ENV" ]    && printf '%s' "$SERVER_AI_ENV"    > /home/ubuntu/Project-Chat-Social/server-ai/.env
          [ -n "$FRONTEND_ENV" ]     && printf '%s' "$FRONTEND_ENV"     > /home/ubuntu/Project-Chat-Social/frontend/.env

      # 4. Deploy
      - name: Deploy on EC2
        run: |
          cd /home/ubuntu/Project-Chat-Social
          docker compose pull
          docker compose up -d --remove-orphans
